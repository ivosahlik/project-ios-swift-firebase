# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'project-firebase' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for project-firebase

  target 'project-firebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'project-firebaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'


end
